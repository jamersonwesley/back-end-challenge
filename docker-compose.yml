version: '3.8'

services:
  app:
    build: .
    container_name: conversor_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: php -S 0.0.0.0:8000 -t src